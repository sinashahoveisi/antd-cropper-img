{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "header-max-length": [0, "always", 50],
    "body-max-line-length": [1, "always", 100],
    "type-enum": [2, "always", ["update", "fix", "test", "build", "feature", "merge", "config"]]
  }
}
